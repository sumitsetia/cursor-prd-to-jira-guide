---
description: Mark a reminding assistant item as done by id or description
---

# Mark reminder done

Mark a requirement as complete.

Match by `REQ-###` id or partial `what` text. Update both OneDrive and `sumit-assistant/requirements.json`. Set `status` to `done` and confirm.
